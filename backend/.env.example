# Database
DATABASE_URL=postgres://user:password@localhost/echo_db

# Auth
JWT_SECRET=your_secure_secret_here

# Server
PORT=3000
HOST=0.0.0.0
RUST_LOG=backend=debug

# FCM Push Notifications (download from Firebase Console → Service accounts)
FCM_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
