services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      # - "443:443" # Uncomment for HTTPS later
    volumes:
      - ./nginx_example.conf:/etc/nginx/nginx.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway" # Required for Linux, good practice generally
